## 问题
既有 `docs/tests/plans/PLAN-STORY9-2026-06-13-delez.md` 的 T1 系列测试用例 (T1-1 ~ T1-7) 全部针对 `src/`，没有一条针对 `.claude/mcp.json` 或 `.claude/mcp.json.example`。T2 系列也未碰 `.claude/mcp.json`。

结果 STORY-9 spec 验收第 6 条「文档不再把 `MCP_SENDER_ONLY` 作为主路径配置」中"主路径配置"明确指 `.claude/mcp.json`，但测试计划完全跳过它 — 导致 PR #39 P0-1 (`.claude/mcp.json` 新增 `MCP_SENDER_ONLY=1`) 漏网，REPORT-STORY9 误判 PASS。

## 影响
- 测试覆盖盲点
- 主路径配置文件未在 STORY-9 测试范围内，spec 验收第 6 条形同虚设
- 下次类似改动还会漏

## 修法
在 `PLAN-STORY9-2026-06-13-delez.md` 加：
- T1-8: `.claude/mcp.json` 不包含 `MCP_SENDER_ONLY`（`grep`）
- T1-9: `.claude/mcp.json` 和 `.claude/mcp.json.example` 的 server 列表一致（除敏感字段外）
- T2-7: `docs/feature-mcp-server.md` 中的配置示例与 `.claude/mcp.json.example` 字段一致

重跑 REPORT 流程，确认覆盖到位。

## 验收
- PLAN + REPORT 更新
- 三条新 case 全过

## 关联
- PR: #39
- Epic: #40
- REVIEW: `docs/tests/REVIEW-STORY9-2026-06-13-xiaoma.md`
