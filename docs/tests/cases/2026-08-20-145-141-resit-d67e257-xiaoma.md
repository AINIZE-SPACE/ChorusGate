# ChorusGate Re-SIT Report — #145 + #141 liveness @ `d67e257`

- **Reviewer**: 小马 (ainizehermes/002, U0B91BVKTL2)
- **Date**: 2026-08-20 08:20–08:40 +0800
- **Branch**: `v5/logging-liveness` @ `d67e257` (= `origin` tip, verified via `git ls-remote`)
- **Scope**: Re-SIT after 打回 — #145 (P0) profile .env 滚动备份+自动恢复；#141 liveness 链（挂起/假活检测 + watchdog + AC3 心跳 + L3 smoke harness + cross-check 归档）
- **Trigger**: 小克 2026-08-20 thread 回复，更正旧 SHA 为 rebase 前本地 SHA，给出真实远端 SHA 组

## 1. SHA 三源核验（先验后测）

| SHA | claim | 核验结果 |
|-----|-------|---------|
| `d67e257` | #145 修复 | ✅ `origin/v5/logging-liveness` tip（`git ls-remote` 一致）；签名代码 `config-init.ts`/`config-migrate.ts` 含 backup/restore 逻辑 |
| `d70f789` | liveness feat | ✅ 在 `origin/v5/logging-liveness` 祖先链 |
| `5204d04` | AC3 心跳测试 | ✅ 同上 |
| `ff79379` | L3 smoke harness | ✅ 同上 |
| `2e60069`/com> | cross-check 归档 | ✅ 同上 |
| `46e1301` | #140 分支头 | ❌ **不在任何远端分支**（远端 `fix/bot-message-mentions` tip = `6a61ecb`，非 #140 内容） |

取证细节（retro 用）: 六个 SHA 的 author time 均为 08-19 早晨，committer time 均为 08-19 11:26 — 与「rebase 重写后旧 SHA 失效、上午产出晚间统一 push」的时间线吻合，支持「漏 push + 汇报旧 SHA」定性，排除「从未存在」。

## 2. Test Execution

环境: Linux zederer-mbe (Ubuntu 26.04), Node v22.22.1, npm 9.2.0, tsx 直接调用。

| Layer | Command | Result |
|-------|---------|--------|
| L0 | `npx tsc --noEmit` | ✅ 0 errors |
| L1 定向 | `tsx --test tests/liveness.test.ts` | ✅ **14/14 PASS** (4 suites, 538ms) |
| 全量回归 | `tsx --test --test-timeout=60000 --test-force-exit tests/*.test.ts` | ✅ **341/345 PASS** — 4 fail = ST-CX-001/002/004/005（codex CLI 未装于本机，`which codex` 确认不存在，已知环境项非回归，08-19 报告已同口径） |
| #136 定向 | `tsx --test tests/config-cli + issue134-agent-config + load-env` | ✅ 65/65 PASS |
| L3 smoke | `node tests/scripts/smoke.mjs`（小克提交的 harness） | ✅ 6/6 PASS（help/status/log tail/log --lines/missing-agent/daemon boot） |

## 3. #145 P0 Root-Cause E2E Verification (temp CHORUSGATE_HOME)

模拟原事故场景（profile 目录被误删 → daemon 无法启动）：

| Gate | Scenario | Result |
|------|----------|--------|
| G1 | `config migrate --apply --from proj.env --agent codex` | ✅ profile `$CH/codex/.env` + backup `$CH/backups/codex.env` 同写，内容 byte-identical |
| G2 | `rm -rf $CH/codex` → `run --agent codex` | ✅ stderr: `[chorusgate] Agent profile "codex" was missing — restored from backup` → profile 重生、backup 保留 |
| G3 | `run --agent nosuch`（无 profile 无 backup） | ✅ fail-closed 引导：`is not initialized / Existing agents: (none) / Check the spelling / --init 提示`，无崩溃 |

结论：**#145 P0 已修复且根因闭环** — 目录级误删可恢复（backup 在 agent 目录外），未初始化 agent 有清晰引导而非崩溃。

## 4. Verdict

- **#141 liveness 链**: ✅ **PASSED** — 全链交付物真实存在（源码 `src/liveness.ts`、14 单测、smoke harness、归档文档），L0/L1/L3 全绿。
- **#145 (P0)**: ✅ **PASSED** — 修复在远端、根因 E2E 验证通过（G1-G3）、回归无新增失败。
- **#136**: ✅ Ready — `212902f` 在本分支祖先链（超集），migration 定向 65/65 绿，最终 SIT 报告即本报告 §2/#136 行, 详见 `2026-08-15-135-resit3-incremental-212902f-xiaoma.md` 的增量基础。

## 5. Outstanding

- **#140**: ❌ 第三起 SHA 不可达 — `46e1301` 不在远端，远端分支头是 `6a61ecb`（#139 内容）。#140 的 3 commits 仍未 push。评审继续 BLOCKED，等真实 SHA。
- 小克承诺的「提交→push→远端核验」完成证明 + 黑事件 retro 补充（补 push events 证据）待落。
- 小克计划把「报 SHA 前先 push + `git branch -r --contains` 核验」落成硬 check 进 chorusgate-code-review skill —— 好提议，建议同步进 dev 侧 skill。

## 6. 附：黑事件定性佐证时间线

author 08-19 09:17–10:56 (feature commits) → committer 08-19 11:26 (rebase rewrite) → 小马 422 三源核验 08-19 晚 → 小克 08-20 更正+push → 小马 08-20 Re-SIT PASSED。全程证据链闭环。

> 报告同步 issue #141 / #145 / #136。#140 待小克真实 SHA。
