# Re-SIT Report — Issue #148/#147 连接健壮性（传输契约对齐 + D1 修复）

> **Issue**: [#148](https://github.com/AINIZE-SPACE/ChorusGate/issues/148)（实施单，挂 #147）
> **Commit**: `d5a006b`（`v5/logging-liveness`，三源核验通过）
> **Previous SIT**: `3824ae0` @ 2026-08-20 CONDITIONAL PASS（P2 #148-D1 + L4 待执行）
> **Test Owner**: 小马 (U0B91BVKTL2)
> **Date**: 2026-08-20
> **Verdict**: ✅ **PASSED**（Linux 侧）— #148-D1 修复确认；传输契约 19/19；WD-5 幂等 14/14；ST-NR-107 profile/token 保留

---

## 0. 执行摘要

| 层 | 内容 | 结果 | 备注 |
|----|------|------|------|
| SHA 三源核验 | ls-remote / branch --contains / git log | ✅ `d5a006b` 一致 | |
| L0 | `npx tsc --noEmit` | ✅ 0 错误 | |
| L1 transport | `transport.test.ts` 20 tests / 7 suites | ✅ 20/20 | 含"无全局 env 变更"断言 |
| L2 全量 | `tsx --test tests/*.test.ts` | ✅ 377 pass / 4 fail / 0 cancelled | 4 fail = ST-CX-001/002/004/005（已知：本机无 codex CLI），非回归 |
| L3 WD-5 | uninstall Linux 幂等（#148-D1 修复） | ✅ 14/14 | `does not exist` 正则匹配 |
| L3 ST-NR-101 | 死代理隔离（direct mode） | ✅ 6/6 | 模块级 smoke |
| L3 ST-NR-102 | CLI 代理继承（inherit/proxy） | ✅ 7/7 | 模块级 smoke |
| L3 ST-NR-106 | 顶层 install/uninstall 幂等 | ✅ 6/6 | round-trip + alias |
| L3 ST-NR-107 | 卸载不删 profile/token | ✅ 3/3 | .env + .token 保留 |
| L4 Windows | ST-NR-101~106 实机 | ⏳ 小克执行中 | 见 §4 |

**缺陷**：0 个 P0/P1/P2（#148-D1 已修复）。

---

## 1. SHA 三源核验

```
git ls-remote --heads origin v5/logging-liveness
  → d5a006b56a4dd9f96eba1da88337c80b23bd8b36

git branch -r --contains d5a006b
  → origin/v5/logging-liveness

git log --oneline d5a006b -8
  d5a006b docs(spec): #147 Status 更新
  5a3c016 feat(#148): 传输契约对齐 — CHORUSGATE_*_TRANSPORT + 无全局 env 变更
  dc9730b merge: 并入远端 #148 SIT §3 根因修正 (5dcd788)
  3345439 fix(#148-D1): uninstallLinux 幂等正则匹配 "does not exist"
  5dcd788 docs(tests): #148 SIT §3 根因修正
```

三源一致。commit chain 真实存在。

---

## 2. #148-D1 修复确认

### 2.1 源码 diff（`src/watchdog.ts` line 156）

```diff
- if (r.status !== 0 && /Unit .* not loaded|not found/i.test((r.stderr || "") + (r.stdout || ""))) {
+ if (r.status !== 0 && /Unit .* does not exist|not loaded|not found/i.test((r.stderr || "") + (r.stdout || ""))) {
```

正则增加 `does not exist`，对齐 systemd 实际输出 + 对齐 `uninstallWindows` 的幂等处理。源码 disable→rm 顺序正确，无需调换。

### 2.2 WD-5 实测（Linux systemd）

```
=== WD-5: uninstall idempotent (2nd) — THE FIX ===
uninstalling watchdog for agent 'sitnr-test'...
✔ watchdog 'chorusgate-watchdog-sitnr-test' not found — nothing to uninstall
PASS: WD-5 uninstall (2nd) idempotent (does not exist matched)
```

**14/14 全绿**：

| 用例 | 验证点 | 结果 |
|------|--------|------|
| WD-1 | install (1st) exit 0 | ✅ |
| WD-2 | timer visible in list-timers | ✅ |
| WD-3 | install (2nd) idempotent | ✅ |
| WD-4 | uninstall (1st) exit 0 | ✅ |
| **WD-5** | **uninstall (2nd) idempotent** | **✅** |
| ST-NR-106a | top-level install exit 0 | ✅ |
| ST-NR-106b | top-level install (2nd) idempotent | ✅ |
| ST-NR-106c | top-level uninstall exit 0 | ✅ |
| ST-NR-106d | top-level uninstall (2nd) idempotent | ✅ |
| ST-NR-106e | watchdog install alias works | ✅ |
| ST-NR-106f | watchdog uninstall alias works | ✅ |
| ST-NR-107a | profile .env preserved | ✅ |
| ST-NR-107b | token file preserved | ✅ |
| ST-NR-107c | token content intact | ✅ |

---

## 3. 传输契约对齐验证（新增 `src/transport.ts` + `tests/transport.test.ts`）

### 3.1 L1 单测：transport.test.ts — 20/20 ✅

| Suite | Tests | 验证点 |
|-------|-------|--------|
| parseTransportMode | 3 | empty→default / case-insensitive / invalid throws |
| slackTransportConfig | 3 | direct default / proxy resolves URL / direct ignores URL |
| agentTransportConfig | 4 | inherit default / legacy GATEWAY_AGENT_PROXY / direct override / proxy+URL |
| buildAgentSpawnEnv | 3 | direct strips proxy / proxy injects URL / inherit unchanged |
| buildSpawnEnv | 3 | inherit keeps env / direct strips / **NEVER mutates process.env** |
| buildSlackAgent | 3 | direct/inherit→undefined / proxy no URL→undefined / proxy→HttpsProxyAgent |
| resolveProxyUrl | 1 | CHORUSGATE_PROXY_URL > GATEWAY_AGENT_PROXY > env |

### 3.2 L3 模块级 smoke：ST-NR-101 + ST-NR-102 — 19/19 ✅

**ST-NR-101（死代理 127.0.0.1:1 隔离）**：

| Check | 结果 |
|-------|------|
| direct mode → no proxy URL in slack config | ✅ |
| direct mode → slack agent undefined (no proxy) | ✅ |
| Slack不受死代理影响 | ✅ |
| direct mode → child env proxy stripped | ✅ |
| direct mode → child env token kept | ✅ |
| buildAgentSpawnEnv does NOT mutate process.env | ✅ |

**ST-NR-102（CLI 代理继承）**：

| Check | 结果 |
|-------|------|
| inherit mode → mode=inherit | ✅ |
| inherit → child env has proxy (http_proxy) | ✅ |
| inherit → child env has proxy (HTTPS_PROXY) | ✅ |
| inherit → child env has botToken | ✅ |
| proxy mode → resolves CHORUSGATE_PROXY_URL | ✅ |
| proxy mode → child env has proxy URL | ✅ |
| CHORUSGATE_PROXY_URL > GATEWAY_AGENT_PROXY priority | ✅ |
| parseTransportMode (empty/PROXY/Inherit/invalid) | ✅ ×4 |
| describeTransport returns string | ✅ |

---

## 4. L4 Windows 实机用例（小克执行中）

以下用例需在 ainize-dev (192.168.1.247) 执行，小马出题、小克操作、小马验收。

| 用例 | 注入 | 预期 | AC | 状态 |
|------|------|------|----|------|
| ST-NR-101 | 死代理 127.0.0.1:1 + `chorusgate start --agent sit-nr` | gateway.log "daemon proxy env stripped — Slack 直连"；Slack auth.test 成功 | AC-1 | ⏳ |
| ST-NR-102 | 死代理 + `GATEWAY_AGENT_PROXY=inherit` + CLI spawn | CLI 子进程 env 含代理；Slack 不受死代理影响 | AC-2 | ⏳ |
| ST-NR-103 | `chorusgate log --error` | error.log 存在 | AC-5 | ⏳ |
| ST-NR-104 | 手动 kill daemon PID | watchdog ≤5min 拉起；error.log 含退出原因 | AC-8 | ⏳ |
| ST-NR-105 | `chorusgate install --agent sit-nr` ×2 → schtasks → uninstall ×2 | 幂等；Task 出现/移除；.env/token 不删 | AC-9 | ⏳ |
| ST-NR-106 | 断网 → 观察 gateway.log 退避 → 恢复 | 退避递增（1s→2s→4s...）；恢复后 ≤2 探测周期收到消息；无重复 | AC-6/7 | ⏳ |

> **注**：Linux 侧 ST-NR-101/102/106/107 已由模块级 smoke + systemd 实测覆盖（见 §2-3）。Windows 侧需实机验证 schtasks 路径 + 端到端代理隔离。

---

## 5. 验收标准对照

| AC | 状态 | 证据 |
|----|------|------|
| AC-1 Slack transport 默认直连，代理不泄漏 | ✅ Linux 模块级 6/6 + 单测 20/20 | §3 |
| AC-2 agent provider 按 profile 选 direct/proxy/inherit | ✅ Linux 模块级 7/7 | §3 |
| AC-3 代理不可用时 Slack direct 可 auth.test + Socket Mode | ⏳ L4 Windows ST-NR-101 | §4 |
| AC-4 direct 不可用→切 proxy 可诊断 | ⏳ L4 | §4 |
| AC-5 unhandledRejection/uncaughtException 有 stack | ✅ Linux 3/3 + 代码审查（前次报告 §2.3） | 前次 |
| AC-6 退避+jitter+熔断+半开恢复 | ✅ 单测 9/9（前次报告 §2.4） | 前次 |
| AC-7 断网恢复 ≤2 探测周期，无重复 | ⏳ L4 ST-NR-106 | §4 |
| AC-8 watchdog 有界重启，无风暴 | ⏳ L4 ST-NR-104 | §4 |
| AC-9 install/uninstall 幂等，不删 profile/token | ✅ Linux 14/14（本次 #148-D1 修复确认） | §2 |
| AC-10 tsc + 测试回归 | ✅ L0 0 err + L1 20/20 + L2 377 pass | §0 |
| AC-11 docs/specs + README 更新 | ✅ spec `docs/specs/issue-147-connection-resilience.md` + transport.ts 注释 | 代码审查 |

---

## 6. 结论

**✅ PASSED（Linux 侧）**：

- **#148-D1 修复确认**：uninstallLinux 正则增加 `does not exist`，WD-5 幂等 14/14 全绿
- **传输契约对齐**：`src/transport.ts` 显式配置契约（CHORUSGATE_SLACK_TRANSPORT / CHORUSGATE_AGENT_PROXY / CHORUSGATE_PROXY_URL），不修改全局 process.env，transport.test.ts 20/20 + 模块级 smoke 19/19
- **ST-NR-101/102/106/107** Linux 侧全绿
- L2 全量 377 pass / 4 known env fail（ST-CX codex CLI 未安装，非回归）/ 0 cancelled

**待完成**：L4 Windows 实机 6 用例（小克执行中，跑完贴证据验收）

**建议路径**：小克完成 L4 Windows → 小马验收 L4 → 小扣关单