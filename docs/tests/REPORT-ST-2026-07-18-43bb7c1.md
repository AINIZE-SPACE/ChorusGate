# 测试报告 — commit `43bb7c1` (#129 #132 #128)

- **测试人**: 小马 (M)
- **日期**: 2026-07-18
- **分支**: `v4/unified-stream`
- **commit**: `43bb7c1 feat: #129 #132 #128 - intermediate output, session map redesign, smart thread reply`
- **改动范围**: 6 files, +796/-19 (.gitignore, gateway.ts, channel-directory.ts, session-context.ts, session-store.ts, profile-config.ts)

---

## 1. 质量闸门

| 项目 | 结果 | 备注 |
|------|------|------|
| `npx tsc --noEmit` | ✅ 通过 | 零错误 |
| `npx tsc` (emit) | ✅ 通过 | dist/ 产物齐全 |
| `npm test` | ⚠️ 146 pass / 1 cancelled | ST-PROV-003 codex spawn 超时 30s（环境问题，非本次回归） |

**注**: 小克报 147/147，实测 146/147。差异是 ST-PROV-003 `generateReply({providerId:codex})` 超时——该测试 spawn codex CLI，与本机环境/codex 可用性相关，非本次三需求改动引入的回归。

---

## 2. #129 中间过程输出 — ✅ 通过

**实现核验**:
- `GATEWAY_PROGRESS_MODE` env 支持 `edit` | `hybrid`(默认) | `append`
- `hybrid` 模式: 进度标签继续 edit placeholder，工具调用/中间结果 append 新消息
- `onStreamUpdate` handler 处理 `tool_call` / `metrics` / `block_start` / `done` 四类事件
- `appendProgressResult()` 有 dedup（`progressMessages.includes(label)`）+ 最大条数限制（`GATEWAY_PROGRESS_MAX_MESSAGES`，默认 5）
- `progressDone` 标志防止完成后继续 append

**代码路径**: gateway.ts L647-770，逻辑完整。
**限制**: 端到端需真 Slack token + 流式 provider，未做 live 测试。静态+逻辑路径核验通过。

---

## 3. #132 session map 重构 — ✅ 通过

**新文件**:
- `src/channel-directory.ts` (196行): `ChannelEntry` / `ChannelDirectory` 类型 + JSON 持久化 + `ChannelDirectoryManager` (ensureProfile/upsertChannel/removeChannel/getChannels/resolveChannelName/lookupChannelType/refreshFromSlack/registerThread)
- `src/session-context.ts` (185行): `SessionContextInfo` / `RoutingContext` 类型 + `buildSessionContextPrompt` / `buildRoutingContextPrompt` / `buildFullContextPrompt` + `buildSessionContext` / `buildRoutingContext` 构建器

**SessionIdentity 扩展**: 新增 `platform` / `chatName` / `chatType` / `userId` / `userName` / `parentChannelId` / `messageId` 字段（metadata，不参与 key 序列化）

**SessionStore 双持久化**:
- JSON (primary): `memory/sessions.json`，version=1，含 key/sessionId/identity/started/lastUsed
- Markdown (legacy compat): `memory/sessions.md` 仍写
- `load()`: 优先 JSON，失败回退 markdown
- **自动迁移**: 旧 markdown 仍可读

**buildPrompt() 注入**: session context + async reply routing 块注入到 agent prompt，agent 知道自己在哪+如何回写（含 `slack_send_message` 工具提示）

**Smoke test 结果** (20 项):
```
=== #128 parseProfileTriggers ===          5/5 ✓
=== #132 ChannelDirectoryManager ===       5/5 ✓
=== #132 session-context builders ===      7/7 ✓
=== #132 SessionStore JSON persistence ===  2/3 (1 设计观察，见下)
```

**观察（非阻塞）**: `persistJson()` 中 `SESSIONS_JSON` 是全局常量（`memory/sessions.json`），而非实例 `sessionsFile` 派生路径。当 `sessionsFile !== SESSIONS_MD`（测试用自定义路径）时，JSON 不写入测试目录。生产环境单实例用默认路径，不影响功能；但影响测试隔离性。建议后续改为 `resolve(dirname(this.sessionsFile), "sessions.json")`。

**.gitignore**: `memory/sessions.json` + `memory/channel-directory.json` 已忽略，`git check-ignore` 确认生效，无敏感数据泄漏。

---

## 4. #128 智能 thread 回复 — ✅ 通过 (Phase 1)

**`shouldReply()` 重构**: 同步 `boolean` → 异步 `Promise<boolean>`，新增 `profileId` 参数。

**多级判断管道**:
| Level | 逻辑 | 实现 |
|-------|------|------|
| L1 | 硬过滤 (subtype/bot/empty) | ✓ 已有，保留 |
| L2 | app_mention + DM | ✓ 已有，保留 |
| L3A | 名字/别名匹配 (`mentionsMyName`) | ✓ 新增，用 `ProfileTriggers` |
| L3B | thread parent 是我们的消息 (`isThreadParentBot`) | ✓ 新增，30s TTL 缓存 |
| L3C | 无其他 bot mention + 我们活跃在 thread (`isActiveInThread`) | ✓ 新增 |
| L4 | LLM 判断 (`llmShouldReply`) | ✓ 新增，`GATEWAY_LLM_REPLY_JUDGE=1` 开启 |

**ProfileTriggers**: `parseProfileTriggers(profileId, botUserId)` 从 `GATEWAY_PROFILE_TRIGGERS_<ID>` env 解析 displayName + aliases。Fallback 用 profileId。

**配置项**:
- `GATEWAY_THREAD_SMART_REPLY` (=0 关闭 L3-4，默认开启)
- `GATEWAY_PROFILE_TRIGGERS_CC=小克,CC,claude`
- `GATEWAY_LLM_REPLY_JUDGE=1` (开启 L4)

**调用链**: `onEvent()` 已改为 async，`await shouldReply(event, profileId)`。

**测试覆盖观察（非阻塞）**: `tests/shouldReply-bot-filter.test.ts` 是 #79 时写的旧测试，重建了 shouldReply 的**纯同步逻辑**（L1-2），未覆盖新的 L3-4 async 逻辑。旧测试仍通过（L1-2 行为未变），但新功能缺单测。建议后续补 L3-4 的单测（mock Slack API + profileMap）。

---

## 5. 结论

| 需求 | 状态 | 评级 |
|------|------|------|
| #129 中间过程输出 | ✅ 实现完整，逻辑正确 | PASS |
| #132 session map 重构 | ✅ 新文件/类型/持久化/prompt 注入全部验证通过 | PASS |
| #128 智能 thread 回复 | ✅ 多级管道完整，Phase 1 达成 | PASS |

**整体**: ✅ **通过，可 push**。

**建议（非阻塞，后续迭代）**:
1. `persistJson()` 的 `SESSIONS_JSON` 路径改为相对 `sessionsFile`，提升测试隔离性
2. 补 L3-4 shouldReply async 逻辑的单测
3. ST-PROV-003 codex 超时测试需排查环境（与本 commit 无关）

---

*报告文件: `E:/my_project/ainize/ChorusGate_dev/docs/tests/REPORT-ST-2026-07-18-43bb7c1.md`*
