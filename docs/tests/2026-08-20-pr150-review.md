# PR #150 Review Report — bot-to-bot message tracking (#144)

- **Reviewer**: 小马 (Hermes, 002) · **Date**: 2026-08-20 20:15 CST
- **Scope**: full review of `v5/issue-144-user-identity-full` @ c126df0 (feat 2f9347d + cleanup c126df0), base `main` @ 2c3ee70
- **Method**: clean worktree `/tmp/cg-pr150`, real merge with main, tsc, full test suite, source inspection.

## 1. Change Summary (verified from diff)
| Area | Change |
|------|--------|
| `src/user-identity.ts` (new, ~150 lines) | `parseUser()` / `isBotMessage()` / `resolveUserIdentity()` — bot_message subtype + `bot_id` (B-prefix) + user prefix detection, message_changed/thread_broadcast author attribution |
| `src/message-handlers.ts` (new, 86 lines) | `MessageHandlerHooks` interface + `defaultMessageHandlerHooks` (structured logging) + `dispatchMessageHandler()` + `botMessageHandlingEnabled()` config knob (`GATEWAY_BOT_MESSAGE_HANDLING=0` disables) |
| `src/gateway.ts` (+32) | dispatch hook wiring at `onEvent()` entry — every event that reaches gateway, including ones shouldReply rejects; `setMessageHandlerHooks()` override for tests/hosts |
| `src/socket-manager.ts` (+16) | stores `user_type`/`bot_id`/`is_bot` on StoredEvent |
| `src/types.ts` (+6) | StoredEvent extension |
| `.env.example` / `README_CN.md` / `docs/gotchas.md` | config knob documented + new gotcha #8 (bot_message identity fields) |
| `tests/user-identity.test.ts` (new, 268 lines) | 14 tests covering all 7 acceptance criteria of #144 |

## 2. Merge Conflict Status: RESOLVED
GitHub reported `mergeable: CONFLICTING`. Real merge test in clean worktree:
- Only 1 conflict file: `.gitignore` (scratch-list duplication — branch added `t2.txt/test-out.txt`, main added same + `min.test.ts` + comment header via 17d05c9).
- Resolution: take main's fuller list (3 lines: `min.test.ts` + section comment). Merge commit `0d019bc`.
- **CRITICAL**: the merge also deletes `min.test.ts` from branch HEAD — it was a stale scratch file from faeb2a6, already removed on main by 17d05c9. Branch-side cleanup c126df0 missed it (only removed t2.txt/test-out.txt). Post-merge state is clean.

## 3. Verification Results (real runs)
| Check | Result |
|-------|--------|
| `tsc --noEmit` on merge result | ✅ 0 errors |
| `tests/user-identity.test.ts` on merge result | ✅ 14/14 pass |
| Full suite `tests/*.test.ts` on merge result | 306 tests, 302 pass / 4 fail |
| Same full suite on branch HEAD c126df0 (pre-merge) | 306 tests, 302 pass / 4 fail — **identical**, fails pre-existing |
| 4 fails | all ST-CX-001/002/004/005 codex-integration (local codex CLI env), documented pre-existing since PR #143 review (17d05c9 report) |

Note: main's suite is 334 tests/4 fail per PR description; post-merge tree runs 306 — because main additionally carries tests from merged #137/#143 not in branch base; 306 = branch-side count (34+1). Numbers differ from PR description's "334" but failure set identical and explained.

## 4. Design Review (第一性原因)
- **Why this design**: bot-to-bot tracking needs an identity layer independent of reply-decision. The PR correctly puts parsing (unconditional) before dispatch (config-gated), so the identity layer is always available for future requirements (#139 bot collaboration, #133 channel response). Separation of parsing/handlers/gateway wiring is clean — no coupling with shouldReply logic.
- **Compatibility**: gateway keeps inline shouldReply (HEAD version), explicitly does NOT depend on #139's extraction refactor. PR description flags the "二选一" — reviewer confirms: if #139 branch merges later, its extraction must rebase onto this PR's dispatch call or conflicts recur. Documented in gotchas.
- **Config default**: ON by default (`GATEWAY_BOT_MESSAGE_HANDLING` unset = enabled). Slight concern: new console.error log lines on every bot/human message could be noisy in production; acceptable for a tracking feature whose defaults are structured logging — ops can set =0.
- **Test quality**: 14 tests cover all 7 acceptance criteria of #144 including negative cases (bot_message without user field, old-style bots with only bot_id, message_changed author attribution, config-off behavior).

## 5. Verdict: **CONDITIONAL PASS — approve merge after trivial conflict fix**
The feature code itself needs NO changes. To merge:
1. Merge `main` into branch (or rebase) — resolve `.gitignore` by taking main's fuller list; this also removes stale `min.test.ts`.
2. Push, confirm GitHub `mergeable: CLEAN`.
3. Then merge PR #150 normally → close #144.

Alternative: I can push the tested merge commit `0d019bc` directly to the branch to unblock — flagging before doing so (parallel-session抢跑 risk noted in memory: check branch tip before push).
